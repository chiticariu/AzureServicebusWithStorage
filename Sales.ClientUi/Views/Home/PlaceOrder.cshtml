@model dynamic

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <div class="card my-4">
                <form action="/home/placeorder" method="POST">
                    <div class="card-body">
                        <h2 class="card-title">OrderId <code>@Model.OrderId</code> sent</h2>
                        <p>Your order has been placed successfully.</p>
                        <p>As a result of the button click:</p>
                        <ul>
                            <li>The <b>ClientUI</b> website will create the order and send a <code>OrderPlaced</code> event.</li>
                            <li>The <b>Billing.Webjob</b> service, which subscribes to <code>OrderPlaced</code>, will receive and process <code>OrderPlaced</code>.</li>
                            <li>The <b>Shipping.Webjob</b> service, which subscribes to <code>OrderPlaced</code>, will receive and process <code>OrderPlaced</code>.</li>
                        </ul>
                        <p>Go look in the <b>Billing</b> and <b>Shipping</b> console windows to see the <code>OrderPlaced</code> event getting processed.</p>
                        <input type="submit" class="btn btn-lg btn-success" onclick="sentMessage()" value="Place another order &raquo;" />
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

